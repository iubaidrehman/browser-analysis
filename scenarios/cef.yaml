scenario: cef

concurrency:
  logical_tasks: 2
  browser_worker_limit: 2
  http_worker_limit: 2

browser:
  engine: cef
  headless: true
  reuse_browser: false

workflow:
  name: minimal

timing:
  warmup_seconds: 2
  measurement_seconds: 10
  cooldown_seconds: 2
